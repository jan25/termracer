### Samples

This sub directory contains
- Each paragraph sample is under /use. These will be used in races
- Paragraph generator/tokenizer under /scripts

Inside scripts dir, run  `sh run.sh` to generate paragraph file under `/try` directory. The selected few will be moved to `/use` directory, which are randomly picked for races.